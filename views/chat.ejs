<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Chat | MindMend</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 min-h-screen flex flex-col">

  <!-- Navbar -->
  <% include ./partials/navbar.ejs %> 


  <!-- Main Chat Section -->
  <main class="flex-grow flex justify-center items-center px-4 py-10">
    <div class="w-full max-w-2xl bg-white rounded-2xl shadow-lg p-6 space-y-4">

      <h2 class="text-2xl font-bold text-center text-gray-700">Mental Health Support Chat</h2>

      <!-- Chat Messages -->
      <div class="h-64 overflow-y-auto border border-gray-300 rounded-md p-4 bg-gray-50">
        <!-- Example messages -->
        <div class="mb-2 text-left text-sm text-gray-800"><strong>You:</strong> I'm feeling overwhelmed.</div>
        <div class="mb-2 text-right text-sm text-blue-600"><strong>Bot:</strong> I'm here for you. Would you like to try a breathing exercise?</div>
        <!-- Messages will be dynamically added here -->
      </div>

      <!-- Input Area -->
      <form action="/chat" method="POST" class="space-y-2">
        <textarea 
          name="message" 
          rows="3" 
          placeholder="Type your message..." 
          class="w-full p-3 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-400 resize-none"
        ></textarea>
        <button 
          type="submit" 
          class="w-full bg-[#00509D] hover:bg-[#003f88] text-white font-medium py-2 rounded-md transition duration-200"
        >
          Send Message
        </button>
      </form>

      <!-- Placeholder Buttons -->
      <div class="pt-4 flex flex-col sm:flex-row gap-3">
        <button class="w-full bg-yellow-400 hover:bg-yellow-500 text-black font-semibold py-2 rounded-md">
          Scan My Face (Coming Soon)
        </button>
        <button class="w-full bg-green-500 hover:bg-green-600 text-white font-semibold py-2 rounded-md">
          Suggest Strategy
        </button>
      </div>

    </div>
  </main>

  <!-- Footer -->
  <% include ./partials/navbar.ejs %> 


</body>
</html>
